<local:AppWindowView x:Class="CanonUtility.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CanonUtility.View"
        xmlns:vm="clr-namespace:CanonUtility.ViewModel"
        mc:Ignorable="d"
        Title="Canon Utility" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <DockPanel>
        <ToolBarPanel DockPanel.Dock="Top">
            <ToolBar>
                <ComboBox ItemsSource="{Binding Cameras}" SelectedItem="{Binding SelectedCamera}" DisplayMemberPath="Name" Width="120"/>
            </ToolBar>
            
        </ToolBarPanel>
        <Grid>
        </Grid>

    </DockPanel>
</local:AppWindowView>
